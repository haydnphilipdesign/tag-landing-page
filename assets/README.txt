Drop your logo here:
- `assets/logo.png` (symbol-only TAG mark used in the hero)
- `assets/logo_text.png` (full logo w/ text used in the footer)

Notes:
- Use transparent PNGs if available.
- If you change filenames/paths, update `index.html`.
